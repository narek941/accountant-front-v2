import styled from 'styled-components/native';

import { Button } from '@ch/components';

export const StyledSubmitButton = styled(Button)`
    height: 40px;
    width: 260px;
    border-radius: 10px;
    align-self: center;
    justify-content: center;
`;
